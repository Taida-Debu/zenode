# Dependencies
node_modules/
.pnpm-store/

# pnpm / turbo
.turbo/
dist/

# Next.js build output (never commit)
.next/
**/.next/

# TypeScript incremental builds
*.tsbuildinfo

# Environment & secrets
.env
.env.*
!.env.example

# OS & editors
.DS_Store
*.swp
.idea/
.vscode/

# Foundry / Solidity
contract/cache/
contract/out/
contract/broadcast/

# Cursor agent instructions (local only)
AGENTS.md

# Local tooling (not for the repo)
scripts/

# Local issue backlog & sync artifacts (never commit)
issues/
